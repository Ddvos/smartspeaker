// Legacy file — kept for backward compatibility with dashboard mock data.
// New code should import from '$lib/data/settings-constants' instead.

export { models as mockModels, voices as mockVoices } from './settings-constants';
